# -*- mode: python -*-

block_cipher = None


a = Analysis(['nodemaze_set_two.py'],
             pathex=['C:\\Users\\jacupan\\Documents\\CodeGolf\\CodeGolf3\\nodemaze_set_two'],
             binaries=None,
             datas=None,
             hiddenimports=[],
             hookspath=[],
             runtime_hooks=[],
             excludes=[],
             win_no_prefer_redirects=False,
             win_private_assemblies=False,
             cipher=block_cipher)
pyz = PYZ(a.pure, a.zipped_data,
             cipher=block_cipher)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='nodemaze_set_two',
          debug=False,
          strip=False,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=False,
               upx=True,
               name='nodemaze_set_two')
